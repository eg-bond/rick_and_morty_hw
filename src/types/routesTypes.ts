export enum AppRoutes {
  Main = '/',
  Locations = 'locations',
  Episodes = 'episodes',
  Characters = 'characters',
  Login = 'login',
  NotFound = '*',
}

export enum Pathnames {
  Locations = '/locations',
  Episodes = '/episodes',
  Characters = '/characters',
}
